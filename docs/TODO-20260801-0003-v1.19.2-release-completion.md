# v1.19.2 — finish the cut

**Priority** P1 · **Blocked by** nothing · **Status** **committed + tagged locally, nothing pushed**

The version-specific half of `RUNBOOK-qa-release.md` §4. The runbook says *what* the steps are and never
names a version; this file is one release walking them. Delete it when the last box is ticked.

Commit `e0635a0`, tag `v1.19.2` (annotated, points at that commit). Nothing is on the remote.

## Done

- `tests/qa.sh` full run — **ALL PASS**, 141 checks, 0 fail, 4 NOTE (all §3e).
- §4 step 1 — `AppInfo.version`, `CHANGELOG.md` dated section. Cut as **PATCH** on the judgment that
  R7 corrects a misleading indicator; read strictly, the new tone and the two `LOG_AWAKE` fields are
  additive observable behavior and argue for MINOR. Dissent is recorded in the CHANGELOG's own Notes.
- §4 step 2 (source half) — `docs/cover.html` prose *and* its Keep Awake demo, which had been driving
  the track line off `running` alone and so rendered a suspended hold identically to `Off`.
- §4 step 3 — `tests/qa.sh --core` re-run green; all four in-repo version surfaces read `1.19.2`.
- §4 step 4 — tagged.

## Open, in order

- [ ] **RUNBOOK §3.1 — perceptibility.** The one thing this release turns on and the one thing no script
      can answer. The paused tone must be distinguishable from **both** the lit and the dark states on a
      2pt line, in a light *and* a dark menu bar. `Tuning.keepAwakeBarPausedAlpha` is `0.22`; retune and
      amend before the tag moves if it does not read. If it cannot be made to read, the honest close is a
      desaturated tone or R7 declined — not shipping an indicator that tests green and shows nothing.
      This is also R7's last open box; ticking it retires that row and deletes its TODO.
- [ ] **Re-run §3e on a quiet machine.** Four NOTEs this run, all from one process holding a display
      assertion throughout (`pmset -g assertions | grep -i display` names it; it was a browser). One of
      them is `tint=paused` — R7's *own* positive case, never observed. The precedence half
      (`paused=1 tint=foreign`) did pass, so this is unverified, not broken.
- [ ] **RUNBOOK §3.2 — menu walk.** Needs an Accessibility grant.
- [ ] **§4 step 2 (deploy half)** — `publish-cover`, then verify the live page serves both the badge and
      the new three-tone prose. Fetch it; the badge being right is not evidence the prose is.
- [ ] **§4 step 5 — push, and pick up the straggler:**
      `git push origin main v1.19.1 v1.19.2`. **v1.19.1 was cut correctly and never pushed** — its
      commit and tag are both local-only, so the remote's newest tag is v1.19.0 and every installed copy
      has been told that is current for two releases.
- [ ] **§4 step 6 — confirm it shipped**, with the command `UpdateChecker` itself polls:
      `git ls-remote --tags origin 'v*' | sed 's|.*refs/tags/||; s|\^{}||' | sort -uV | tail -3`

## Fallout worth landing separately

- [ ] `ROADMAP.md` § Release hygiene says *"tagging stays the one manual step to get right."* The
      straggler above disproves it — pushing is a second, and the two fail differently: a bad tag is
      visible locally, an unpushed one is invisible everywhere except the remote. That section should
      also gain the demo-reproduces-a-fixed-bug failure mode, which is not prose drift and is not
      covered by the R14/R15 tally beside it.
