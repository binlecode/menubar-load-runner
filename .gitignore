MenuBarLoadRunner
# The launcher's build temp, renamed over the binary on success. Listed explicitly — a
# `MenuBarLoadRunner*` glob would swallow MenuBarLoadRunner.swift, the only source of truth.
MenuBarLoadRunner.new
tmp/
.wrangler/
.claude/settings.local.json

# Symlink into the private vault (strategy / research / deep design). Never committed.
docs/design-docs
